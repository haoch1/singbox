# singbox 管理脚本说明

## 定位

`singbox.sh` 是面向 Linux VPS 的单文件管理脚本，负责 sing-box 核心、VLESS + Reality + Vision 节点和 Shadowsocks 2022 节点的生命周期管理。脚本仅使用 Bash、系统命令和已声明的管理工具，不依赖额外运行时库。

## 行为边界

脚本对外提供以下稳定接口：

- `s`、`s --update`、`s --update-script`、`s --version` 和 `s --uninstall` 命令；
- 菜单编号、交互提示、成功或失败状态和退出码；
- `/usr/local/etc/sing-box/config.json` 与 `nodes.json` 的现有字段和协议参数；
- systemd、OpenRC 及 direct 三种服务管理路径。

优化不改变上述接口，也不改变下载地址、核心安装路径、服务单元路径、日志策略或卸载范围。

## 内部实现

节点字段读取使用一次 `jq` 生成 NUL 分隔记录，避免同一节点在生成链接和修改页面中重复启动多个解析进程。脚本写入路径已拒绝控制字符，NUL 可安全作为受支持节点的字段边界；读取端仍按原有命令替换规则移除末尾换行。

临时文件清理将同一目录的匹配条件合并到一次 `find` 遍历。菜单行宽度计算合并 ANSI 序列剥离和 UTF-8 宽度计算，减少外部进程数量。配置变更仍沿用临时文件、核心校验、服务重启和失败回滚顺序。

## 安全与数据策略

配置和节点元数据使用受限权限保存。更新流程在替换核心或管理脚本前验证下载内容、版本和 SHA256（服务端提供摘要时）；事务失败保留恢复备份。卸载仅清理脚本创建的文件，不删除系统预先存在的外部核心和共享依赖。
